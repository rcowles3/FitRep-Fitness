<div class="container">
    <div class="row">
        <div class="col-md-4"></div>
		<div class="col-md-4">
			<h1>User List</h1>
             <li>
                {{#each user}}
                    <div>First Name: {{this.first_name}}</div>
                    <div>Last Name:  {{this.last_name}}</div>
                {{/each}}
            </li> 
		</div>
		<div class="col-md-4">
            
        </div>
    </div>
</div>